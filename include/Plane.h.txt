#ifndef PLANE_H
#define PLANE_H

#include "Shape.h"

namespace Raytracer148 {
class Plane : public Shape {
public:
    Sphere(Eigen::Vector3d &center, Vector3d &normal) : c(center), n(normal) {}

    virtual HitRecord intersect(const Ray &ray);

private:
    Eigen::Vector3d c,n;
    
};
}

#endif
